# Eval fixture: bind the embedded Tomcat listener to the PORT environment
# variable (default 8080), satisfying the fixture contract. Spring resolves
# ${PORT} from the process environment.
server.port=${PORT:8080}
