# Eval fixture: deliberately uninstrumented Ruby HTTP service (see
# evals/fixtures/README.md for the contract). A plain Rack application served
# by WEBrick; Net::HTTP performs the outbound call.
source 'https://rubygems.org'

gem 'rack', '~> 3.1'
gem 'rackup', '~> 2.2'
gem 'webrick', '~> 1.9'
